apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: awseks-backend
  namespace: argocd
spec:
  project: default
  source:
    # 백엔드 GitHub 리포지토리 주소
    repoURL: 'https://github.com/pjcosmos/aws.git'
    targetRevision: HEAD
    # 리포지토리 내의 매니페스트 파일 경로 (루트 폴더일 경우 '.')
    path: 'backend/k8s'
  destination:
    server: 'https://kubernetes.default.svc'
    namespace: default
  syncPolicy:
    automated:
      prune: true
      selfHeal: true